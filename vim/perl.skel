#!/usr/bin/env perl

use strict;
use warnings;
use utf8;

use lib::abs qw( ./lib ../lib );

use Data::Dumper;

use EV;
use AnyEvent;
use AnyEvent::HTTP;

use JSON::XS;
our $JSON=JSON::XS->new->utf8;


EV::loop;
